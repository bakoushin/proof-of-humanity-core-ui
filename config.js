const port = process.env.PORT;
const providerURL = process.env.PROVIDER_URL;
const coreAddress = process.env.CORE_ADDRESS;
const chainId = process.env.CHAIN_ID;

export { port, providerURL, coreAddress, chainId };
